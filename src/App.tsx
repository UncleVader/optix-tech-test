import {Container} from "@mui/material";
import {HomePage} from "./components/HomePage";

export const App = () => {
  return (
    <Container maxWidth={"md"}>
      <HomePage/>
    </Container>
  )
}